{{!< default}}

{{#author}}
<header class="gh-hero">
    {{#if cover_image}}
        <img class="gh-hero-img" src="{{cover_image}}" alt="{{name}}" />
    {{/if}}
    <div class="gh-hero-content">
        {{#if profile_image}}
            <img class="gh-author-image" src="{{profile_image}}" alt="{{name}}" />
        {{/if}}
        <h1>{{name}}</h1>
        {{#if bio}}
            <p>{{bio}}</p>
        {{/if}}

        <div class="gh-author-meta">
            <div class="gh-author-links">
                {{#if website}}
                <a href="{{website}}" target="_blank" rel="noopener">Website</a>
                {{/if}}
                {{#if twitter}}
                <a href="{{twitter_url}}" target="_blank" rel="noopener">Twitter</a>
                {{/if}}
                {{#if facebook}}
                <a href="{{facebook_url}}" target="_blank" rel="noopener">Facebook</a>
                {{/if}}
            </div>
        </div>
    </div>
</header>
{{/author}}

<div class="gh-page">
    <div class="gh-container">

        <div class="gh-postfeed">
        {{#foreach posts}}

            {{> "card"}} {{!-- partials/card.hbs --}}

        {{/foreach}}
        </div>

    </div>
</div>